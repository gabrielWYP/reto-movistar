services:
  back:
    build:
      context: .
      dockerfile: back/Dockerfile
    environment:
      SONIA_ENVIRONMENT: local
      SONIA_LOG_LEVEL: INFO
      SONIA_BI_MODEL: ${SONIA_BI_MODEL:-deepseek-v4-flash}
      OPENCODE_KEY: ${OPENCODE_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      SONIA_COLLECTIONS_MODEL: ${SONIA_COLLECTIONS_MODEL:-}
      SONIA_COLLECTIONS_REASONING_EFFORT: ${SONIA_COLLECTIONS_REASONING_EFFORT:-}
      SONIA_COLLECTIONS_MAX_TOOL_CALLS: ${SONIA_COLLECTIONS_MAX_TOOL_CALLS:-}
      SONIA_COLLECTIONS_MAX_OUTPUT_TOKENS: ${SONIA_COLLECTIONS_MAX_OUTPUT_TOKENS:-}
      SONIA_COLLECTIONS_MAX_UPLOAD_BYTES: ${SONIA_COLLECTIONS_MAX_UPLOAD_BYTES:-26214400}
      SONIA_COLLECTIONS_MAX_UPLOAD_FILES: ${SONIA_COLLECTIONS_MAX_UPLOAD_FILES:-6}
    read_only: true
    restart: unless-stopped
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m

  front:
    build:
      context: .
      dockerfile: front/Dockerfile
    depends_on:
      back:
        condition: service_healthy
    environment:
      BACKEND_HOST: back
      BACKEND_PORT: "8080"
    ports:
      - "${SONIA_PUBLIC_PORT:-8080}:8080"
    restart: unless-stopped
