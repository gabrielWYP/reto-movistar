[project]
name = "sonia-billing-agent"
version = "0.1.0"
description = "Deterministic, evidence-first billing assurance agent for SON-IA."
requires-python = ">=3.11"

[project.scripts]
sonia-billing = "billing_agent.cli:main"
sonia-billing-web = "billing_agent.web_app:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
