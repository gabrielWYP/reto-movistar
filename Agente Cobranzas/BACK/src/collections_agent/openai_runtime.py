"""OpenAI Responses integration with deterministic, local function tools."""  from __future__ import annotations  import json import os from typing import Any  from .agent import SYSTEM_PROMPT, dispatch from .config import Settings from .service import CollectionsService   def _schemas() -> list[dict[str, Any]]:     """Expose only the five calculations the model may select."""     return [         {             "type": "function",             "name": "portfolio_snapshot",             "description": "Obtiene KPIs, saldo vencido y antiguedad de la cartera.",             "parameters": {"type": "object", "properties": {"as_of_date": {"type": "string"}}, "additionalProperties": False},         },         {             "type": "function",             "name": "customer_snapshot",             "description": "Analiza la situacion de cobranza de un cliente.",             "parameters": {"type": "object", "properties": {"customer_id": {"type": "string"}, "as_of_date": {"type": "string"}}, "required": ["customer_id"], "additionalProperties": False},         },         {             "type": "function",             "name": "invoice_trace",             "description": "Reconstruye pagos, creditos, saldo y estados de una factura.",             "parameters": {"type": "object", "properties": {"document": {"type": "string"}, "as_of_date": {"type": "string"}}, "required": ["document"], "additionalProperties": False},         },         {             "type": "function",             "name": "collection_priorities",             "description": "Obtiene un ranking explicable para la gestion de cobranza.",             "parameters": {"type": "object", "properties": {"limit": {"type": "integer", "minimum": 1, "maximum": 50}, "as_of_date": {"type": "string"}}, "additionalProperties": False},         },         {             "type": "function",             "name": "reconciliation_exceptions",             "description": "Lista pagos, facturas o notas de credito que requieren validacion.",             "parameters": {"type": "object", "properties": {"limit": {"type": "integer", "minimum": 1, "maximum": 50}, "as_of_date": {"type": "string"}}, "additionalProperties": False},         },     ]   def _client(api_key: str) -> Any:     """Load the official SDK only for a requested conversational analysis."""     try:         from openai import OpenAI     except ImportError as error:         raise RuntimeError("Falta el SDK de OpenAI. Instala las dependencias del backend antes de activar la IA.") from error     return OpenAI(api_key=api_key, timeout=60.0, max_retries=2)   def _post(client: Any, payload: dict[str, Any]) -> dict[str, Any]:     try:         return client.responses.create(**payload).model_dump(mode="json")     except Exception as error:         raise RuntimeError("OpenAI no pudo completar la consulta. Revisa la clave, el acceso al modelo y la conexion.") from error   def _function_calls(response: dict[str, Any]) -> list[dict[str, Any]]:     return [item for item in response.get("output", []) if item.get("type") == "function_call"]   def _usage_total(responses: list[dict[str, Any]]) -> dict[str, int]:     totals: dict[str, int] = {}     for response in responses:         for key, value in response.get("usage", {}).items():             if isinstance(value, int):                 totals[key] = totals.get(key, 0) + value     return totals   def ask(service: CollectionsService, question: str, model: str | None = None) -> dict[str, Any]:     """Let the model choose tools; all money and status calculations stay local."""     api_key = os.environ.get("OPENAI_API_KEY")     if not api_key:         raise RuntimeError("La consulta con IA requiere OPENAI_API_KEY. Los analisis deterministas siguen disponibles sin esa clave.")     if not question.strip():         raise ValueError("Escribe una consulta para el agente.")      settings = Settings.from_env()     selected_model = model or settings.model     client = _client(api_key)     responses: list[dict[str, Any]] = []     response = _post(client, {         "model": selected_model,         "store": False,         "instructions": SYSTEM_PROMPT,         "input": question,         "tools": _schemas(),         "tool_choice": "auto",         "reasoning": {"effort": settings.reasoning_effort},         "max_output_tokens": settings.max_output_tokens,     })     responses.append(response)     tool_results: list[dict[str, Any]] = []      for _ in range(settings.max_tool_calls):         calls = _function_calls(response)         if not calls:             break         outputs: list[dict[str, str]] = []         for call in calls:             try:                 arguments = json.loads(call.get("arguments", "{}"))                 result = dispatch(service, call["name"], arguments)             except (json.JSONDecodeError, KeyError, TypeError, ValueError) as error:                 result = {"error": f"No se pudo ejecutar la consulta solicitada: {error}"}             tool_results.append({"tool": call.get("name", ""), "result": result})             outputs.append({"type": "function_call_output", "call_id": call["call_id"], "output": json.dumps(result, ensure_ascii=False)})         response = _post(client, {             "model": selected_model,             "store": False,             "instructions": SYSTEM_PROMPT,             "previous_response_id": response["id"],             "input": outputs,             "tools": _schemas(),             "tool_choice": "auto",             "reasoning": {"effort": settings.reasoning_effort},             "max_output_tokens": settings.max_output_tokens,         })         responses.append(response)      if _function_calls(response):         raise RuntimeError("La consulta requiere mas pasos de los permitidos. Formula una pregunta mas especifica.")     return {"answer": response.get("output_text", ""), "tool_results": tool_results, "usage": _usage_total(responses), "model": selected_model}