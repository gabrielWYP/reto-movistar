# Interfaz del MVP

SPA estática, sin toolchain Node, servida por FastAPI desde el mismo contenedor. Consume únicamente `/api/demo/*` y conserva el contrato de entrada única.

La interfaz simula un journey controlado con datos ficticios. No persiste casos, no emite facturas reales y no aplica pagos.
