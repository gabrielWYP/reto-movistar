"""Runtime configuration loaded from environment variables only.  Secrets are never stored in this module or in repository files.  The OpenAI key is read only when a conversational request is made. """  from __future__ import annotations  import os from dataclasses import dataclass   def _positive_int(name: str, default: int, maximum: int) -> int:     try:         return max(1, min(int(os.environ.get(name, default)), maximum))     except ValueError:         return default   @dataclass(frozen=True, slots=True) class Settings:     model: str     reasoning_effort: str     max_tool_calls: int     max_output_tokens: int     max_upload_bytes: int     max_upload_files: int     host: str     port: int      @classmethod     def from_env(cls) -> "Settings":         effort = os.environ.get("SONIA_REASONING_EFFORT", "low").lower()         if effort not in {"none", "low", "medium", "high", "xhigh", "max"}:             effort = "low"         return cls(             model=os.environ.get("SONIA_MODEL", "gpt-5.6-terra"),             reasoning_effort=effort,             max_tool_calls=_positive_int("SONIA_MAX_TOOL_CALLS", 3, 5),             max_output_tokens=_positive_int("SONIA_MAX_OUTPUT_TOKENS", 700, 2000),             max_upload_bytes=_positive_int("SONIA_MAX_UPLOAD_BYTES", 10 * 1024 * 1024, 50 * 1024 * 1024),             max_upload_files=_positive_int("SONIA_MAX_UPLOAD_FILES", 6, 6),             host=os.environ.get("SONIA_HOST", "0.0.0.0"),             port=_positive_int("SONIA_PORT", 8080, 65535),         )