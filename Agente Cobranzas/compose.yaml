services:   back:     build:       context: .       dockerfile: BACK/Dockerfile     environment:       SONIA_DATASET_PATH: /data/source/SONIA_DESAFIO_03.zip       SONIA_ENVIRONMENT: local       SONIA_MODEL: gpt-5.6-terra       SONIA_REASONING_EFFORT: low       OPENAI_API_KEY: ${OPENAI_API_KEY:-}     volumes:       - ./data:/data:ro     read_only: true     tmpfs:       - /tmp    front:     build:       context: .       dockerfile: FRONT/Dockerfile     environment:       BACKEND_HOST: back       BACKEND_PORT: 8080     ports:       - "8080:8080"     depends_on:       back:         condition: service_healthy