[project]
name = "sonia-bi-agent"
version = "0.1.0"
description = "Evidence-first BI Agent for SON-IA."
requires-python = ">=3.11"

[project.scripts]
sonia-bi = "bi_agent.cli:main"
sonia-bi-web = "bi_agent.web_app:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
