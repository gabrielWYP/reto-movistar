[project]
name = "sonia-bi-agent"
version = "0.1.0"
description = "Deterministic business-intelligence core for SON-IA."
requires-python = ">=3.11"

[project.scripts]
sonia-bi = "bi_agent.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
