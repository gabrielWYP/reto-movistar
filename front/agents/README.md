# Módulos visuales de agentes

Cada subcarpeta contiene la configuración visual que corresponde al agente del
mismo nombre en `back/src/sonia/agents/`. El navegador carga los tres módulos
antes de `assets/app.js`; sus identificadores son parte del contrato entre pods.
