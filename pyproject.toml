[project]
name = "sonia-collections-agent"
version = "0.1.0"
description = "Deterministic collections and payment-reconciliation agent for SON-IA."
requires-python = ">=3.11"

[project.scripts]
sonia-collections = "collections_agent.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

